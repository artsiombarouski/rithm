import { Stack } from 'expo-router';

const Layout = () => {
  return (
    <Stack
      screenOptions={{
        title: 'Movie info',
        headerTitle: 'Movie info',
      }}
    />
  );
};

export default Layout;
