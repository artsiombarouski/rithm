import { Stack } from 'expo-router';

const Layout = () => {
  return (
    <Stack
      screenOptions={{
        title: 'TV info',
        headerTitle: 'TV info',
      }}
    />
  );
};

export default Layout;
