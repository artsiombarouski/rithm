{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "useWorkspaces": false,
  "version": "independent",
  "npmClient": "yarn",
  "command": {
    "publish": {
      "registry": "https://npm.pkg.github.com/"
    },
    "version": {
      "allowBranch": "main",
      "ignoreChanges": [
        "**/*.md"
      ]
    }
  },
  "packages": [
    "packages/*"
  ]
}
